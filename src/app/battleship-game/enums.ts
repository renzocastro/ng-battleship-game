export enum ShipDirection {
  Horizontal,
  Vertical
}

export enum TileState {
  Open,
  Close
}

export enum Difficult {
  Easy = 0,
  Normal = 100,
  Hard = 50
}

export enum Player {
  One,
  Two,
  None
}
